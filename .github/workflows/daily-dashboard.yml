name: Daily YouTube Dashboard

on:
  schedule:
    # 12:01 AM Pacific (8:01 UTC) on Tuesday and Saturday
    - cron: '1 8 * * 2,6'
  workflow_dispatch: # Allow manual trigger from GitHub UI

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Restore previous snapshots
        uses: actions/cache@v4
        with:
          path: output/data
          key: yt-snapshots-${{ github.run_number }}
          restore-keys: |
            yt-snapshots-

      - name: Generate YouTube dashboard
        env:
          YOUTUBE_API_KEY_2: ${{ secrets.YOUTUBE_API_KEY_2 }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          LLM_PROVIDER: ${{ secrets.LLM_PROVIDER || 'openai' }}
        run: python youtube_dashboard.py

      - name: Commit dashboard outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          mkdir -p output/data
          git add output/dashboard.html output/data/latest.json
          git diff --cached --quiet || git commit -m "chore: update dashboard outputs [skip ci]"
          git push

      - name: Prepare Pages site
        run: |
          mkdir -p _site
          cp output/dashboard.html _site/index.html
          cp output/digest-*.html _site/ 2>/dev/null || true
          cp -r output/data _site/data 2>/dev/null || true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
